#include <time.h>

#include "Arene.h"

int main(int argc, char** argv) {
    // Initialisation de l'aleatoire
    srand(time(NULL));

    // Execution de l'arene
    arene();

    return EXIT_SUCCESS;
}
